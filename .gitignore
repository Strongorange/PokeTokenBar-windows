# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# App bundle build output
build/

# OMC / agent local state
.omc/

# macOS
.DS_Store
*.xcuserstate

# Signing material — never commit private keys / certs
*.p12
*.pem
*.key
*.cer
*.certSigningRequest

# Project documentation is versioned with the repository.

# Agent worktrees / local state
.claude/
.worktrees/

# Local-only review-gate opt-out (not published)
.no-review-gate

# .NET (Windows port)
bin/
obj/
artifacts/
*.user
